// middleware for logout endpoint
const logout = async (req, res) => {
  // TODO(ca): implements blacklist token feature
  // get token
  // decode token
  // verify signature token
  // set to true user blacklist attr
  res.json({});
}

module.exports = logout;